# Uninstall

```sh
github-agent-auth uninstall
rm "$HOME/.local/bin/github-agent-auth"
```

This removes the local service, protected files, logs, wrappers, Git configuration, shell
path block, and worker account.

Remove the external GitHub App separately in GitHub settings.
